import { DateFormatStringPipe } from './date-format-string.pipe';

describe('DateFormatStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatStringPipe();
    expect(pipe).toBeTruthy();
  });
});
